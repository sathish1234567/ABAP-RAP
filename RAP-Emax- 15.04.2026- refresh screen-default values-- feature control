Action: to update the multiple records - click on ModifyStatus button.- when you providing the status on popup.

zr_dbt_trav- root entity.
ZDBT_TARV- Database table
zstatus_str - structure


define root view entity ZR_DBT_TRAV
  as select from ZDBT_TRAV as r_dbttrav
  {
    kay travel_id as TravelID,
    description as Description,
    Status as Status,
    @Semantics.systemDateTime.localInstanceLastChangedAt: true
    lastchangedat as Lastchangedat
    createdby as CreatedBy,
    createdat as CreatedAt,
    lastchangedBy as lastchangedby
  }

zstatus_str:
____________
define strucutre zstatus_str {
  status : /dmo/travel_Status
}

Projection view:
_____________
projection implementation in calss zbp_c_dbt_Trav unique;

strict( 2 );
extensible ;
use draft;
use side effects;
define behavior for zc_dbt_Trav alias r_dbt_trav
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action edit;
  use action activate;
  use action discard;
  use action resume;
  use action prepare;

  uae action UpdateStatus;

}
____________________
managed implementation in class z*********** unique;
strict( 2 );
with draft;
extensible;
defaine behavior for zr_dbt_trav alias R_DBT_TRAV
persistent table ZDBT_TRAV
extensible
draft table ZDBT_TRAV_D
etag master LastchangedAt
lock master total etag LastchangedAt
authotization master( global )
{

  field ( mandatory : create )
    TravelID;

  field (readonly )
    Lastchengedat;

  field( readonly : update )
   TravelID;

  create:
  update;
  delete;

  draft action  Activate optimized;
  draft action Discard;
  draft action Edit;
  draft action Resume;
  draft determine action Prepare;
  action UpdateStatus parameter zstatus_str  --> ctrl + 1 --> create structure

  mapping for zdbt_trav corresponding extensible 
  { 
    TravelID = TRAVEL_ID;
    Description = DESCRIPTION;
    status = STATUS;
    Lastchangedat = LASTCHANGEDAT;
    CreatedBy = CREATEDBY;
    Lastchangedby = LASTCHANGEDBY;

  }
  }

behavior implementation class:
_____________________________

class lhc_zr_dbt_trav definition inheriting from cl_abap_behavior_handler.
private section.
  methods : get_global_authentications for global authorization
            importing request requested_authorizations for r_dbt_Trav
            result result,

            updatestatus for modify
                importing keys for action R_DBT_TRAV~UpdateStatus .
endclass.
class lhc_zr_dbt_trav implementation.
  method get_global_authentications.
  endmethod.

  method updatestatus.
    modify entities of zr_dbt_trav in local mode entity r_dbt_trav
    update fields ( Status )
    with value #( for key in keys ( %key-TravelId = Key-TravelId
                                    status = key-%param-status ) ) 
    reported data( lt_reported )
    failed data( lt_failed ).
  endmethod.
endclass.

Output:
Selected 2 records from output --> clikc on modify status --> popup appears -- with status - T( input value ) -- 
clikc on modifystatus button --> it will update the status 'T" to selected records. 
Travel status updated 'T' with multiple records.

Scenario:
_________________
- after updation - currently our travel status is 'T'- when i am trying to update - to 'R', -- clikc on modify status -->
immediately the result after updation od db will not return immediately to the UI, --> it will return when click on "GO' button.
Once click on GO button- 'R" update...

but these functionality we have to overwrite -- after clicking on ModifyStatus button --> 
data should be update and immediately visible to the user at ui level ( Not required to click the GO button)

Solution:
_________
- need to add the result paramenter in action.- means whetever updated values to the return- immediately.

action UpdateStatus parameter zstatus_Str  result[1] $self;
here:
$self - it represents the current entity. means which entity return   the results.

class- changes-lhc_zr_dbt_trav
__________________
  methods : updatestatus for modify 
            importing keys for action R_DBT_TRAV~UpdateStatus  result result. <-- add the result parameter in method interface

Class implementations: after changing we got the ne value right.. in updatestatus method implementation.:
________________
  1. modify enitties --> update the data to DB- ZR_DBT_TRAV
  2. after update the data - we have to immediately read the data using read enitities.
  3. if you read selected entity, you will get the update value- whatever you are getting - you have to pass through that result parameter.

  Dynamic value you to get : keys

  read entities of zr_dbt_trav in local mode enitity r_dbt_Trav 
  all fields with corresponding #( keys ) data( result ). --> once get the data -- i am move it into the result parameter- defined in interface.

  result = lt_Result.( send key data in %tky )

  Ex: 
  class lhc_zr_dbt_trav definition inheriting from CL_ABAP_BEHAVIOR_HANDLER.
    private section.
      methods : update status for modify 
                importing keys for action R_DBT_TRAV~updatestatus rsult reult.
  endclass.

  class lhc_zr_dbt_trav implementation.
    method updatestatus.
        // records updated in db.
        modify entities of zr_dbt_trav in local mode entity r_dbt_trav
        update fields ( Status )
        with value #( for key in keys ( %key-TravelId = Key-TravelId
                                        status = key-%param-status ) ) 
        reported data( lt_reported )
        failed data( lt_failed ).

        // once updated- again read those records and send to the output.
        read entities of zr_dbt_trav in local mode enitity r_dbt_Trav 
        all fields with corresponding #( keys ) data( result ). 

        result = value #( for ls_result in lt_Result( %tky = ls_result-%tky
                                                      %param = ls_result ).

    endmethod.
  endclass.

save and activate

output:
_________
now select records and  click on 'modifystatus' button --> provide status - E in popup --> click on update status..
-- now records will update in db after that.. frontend side result will reflect immediate through the custom action.

*********************************************************************
*********************************************************************
*********************************************************************


Scenario: to provide multiple fields on popup -- on custom button action.
___________________________________________________________________________

Just add the one more field inside zstatus_Str.

define structure zstatus_str
{
status : /dmo/travel_Status;
description : /dmo/description;
}

output
__________
now refresh the screen from front end..  --> select multiple records --> click modifystatus button -->
popup appears with two inout bixed 1. status 2. Description.
and enter the data --> click on update...


*********************************************************************
*********************************************************************
*********************************************************************

Scenario: Button adding to each row.. - modify status button:
_____________________________________________________________

in projection view - go to custom action- -- use the --> inline :true in custom action ui annotation.
custom action here is :
@UI.lineItem: [{position: 10 type: #for_Action dataAction: 'UpdateStatus' label : 'ModifyStatus' inline true }].

Now refresh the screen at front end --> check --> 'ModifyStatus' button added or not in each row.

Ouput:
__________
'ModifyStatus'- button added on every record.


*********************************************************************
*********************************************************************
*********************************************************************

Scenario:
Whenever going for the creation of a record- need to fill the default value in travelStatus UI input element in object page.
________________________________________________________________________________________________________________________
- to get the default values to ui elements- to implement the functions. and function name is - ' Deafult Function '
- to implement that we have an option called as  -Deafult Function  and function name should be provide as per nameing guide lines.
- Default function name is - 
Ex:
  for create : getdetafaultforCreate

in behavior definition: 

create : { default function GetDefaultForCreate ; ) -->select the method  ctrl + f1 --> click on 
// here you are implementing during create operation.

method definition for getdefaultforcreate:

methods : getdefaultforcreate for read
            importing keys for action R_DBT_TRAV~getdefaultforcreate result result.

methond implementation:
method getdefaultforcreate. // to get the interface of the method -- select the method - press f2 - it will show all the interface properties.
  append value #( %cid= keys[1]-%cid 
                  %params-status = 'B' ) to result.  
endmethod.

save and active.- test. -- default value will not appear because -- need to expose the function in the projection view also.

in projection view expose function : 
{
use function getdefaultforcreate;
}

save and activate test.. - now you get the data at ui level.

when i click on create button-- >getdefaultcreate - method will trigger. to get the default values. on create action.

default function parameters are -
impporting keys type table for function import zr_dbt_Trav\\r_dbt_trav~getdefaultforcreate
chnaging result type table for function result zr_dbt_Trav\\r_dbt_trav~getdefaultforcreate
         failed type response for failed entity zr_dbt_Trav
         reported type response for reported entity zr_dbt_Trav.

keys( internal table ) - components are -
  |_ %cid type abp_behv_cid
result- components are
  |_ %cid
  |_ %param
        |_ enity fields are inherit here..
              travelid, description, status, lastchangedat, createdby, createdat, lastchangedby.


Note : functions are limited to create operation only...
if you want to provide default values on update operation-- we have another way....  i.e determination- concept..

*********************************************************************
*********************************************************************
*********************************************************************

scenario: in RAP- field should be read only.
_______________________________

in behavior definition:
{
field ( mandatory : create ) --> on create - traval id is mandatory
travelId;

field ( readonly ) -- > read only
status;

field ( readonly: update ) -- > read only
travelId;
}

*********************************************************************
*********************************************************************
*********************************************************************
Scenario: Can i enabled the action on specific conditions.? using feature control

in fiori app -- initial load some of the buttons are disabled -- such as modify status/Delete actions are disabled..
when i select the any record --> those buttons are automatically enabled. 

but my requirement is when the travel status = 'B' then only modify status button should be enabled.

Feature control: enable/disable...
_______________
you can define for custom action, create, update and delete.. and you can refer the individual fields also...

ex: i want to make it read only... every time is make it read only...
specific conditions also...

in this feture control.. you can define it for your actions, fields... , for operations( standard operation- CRUD)

we have  two differnt types of feature controls..
1. static feature control  - without condtion - (features : global ) 
2. dynamic/instance feature control- specific condition- (features : instance )

static feature control: ( featurs : global )
-  Global feature control.
- it enables the all the things..
- it enlables/ disables all the features irrespective of the confrions.
- it should not enabled based on specific conditions.
- if you implement this - it will geenrate the specific method: get_global_features is not implemented.
  global class - zbp_r_dbt_trav --> local handler class - lhc_zt_dbt_trav.

-
Whenever you want any operation should be enabled/disabled as a gloablly-- 
you have to implement the global feature menas static feature.

specific operation enabled/ disabled --globally ... 
 in behavior implementation:

{

create ( features : global )
update ( feauters : global )
delete ( features : global )

}

and define the method : get_global_features

signature : 
methods : get_global_features for global features
          importing request request_features for r_dbt_trav result result.


method implementation:

method get_global_feature. -- select this - f2 get the all interface of this method.
  
  result-%update = if_Abap_behv~fc-o-disabled; (edit button disabledd)
endmethod.

output:
__________
in object page - - edit button disabled... not allowed any record to update globally... 
Disabled the delete/edit buttons..

get_global_features (Dynamic structure/components availble importing and changing )
  |_ importing requested_features type structure for global features request zr_dbt_trav\\r_dbt_trav
              here derived type dynamically changed -- suppose applied global feature for update- update appears.
              if applied for 3 operations -- those all are appears here 
              requested_features- components
                |_ %update %create, %delete
     changing result type structure for global features request zr_dbt_trav\\r_dbt_trav optional
              reported type response for reported only zr_dbt_trav.

if_Abap_behv~fc ( function code )
            ~fc-o- function code - operation
            ~fc-o-enabled( enabled the button ) type abp_behv_op_ctrl value '00'
            ~fc-o-disabled( disabled the button ) type abp_behv_op_ctrl value '01'


scanrio: i have to apply the enabled/disabled for specific condition based..- instance feaures...(Dynamic control )
_____________________________________________________________________________

in behavior definition:

{

delete( features : instance )  --> method :get_instance_features

}

methods signature:

 methods : get_instance_features for instance features 
           importing keys request requested_features for r_dbt_trav result result.

      " here keys - not availble in global features, availbe in insatnce only.
        keys - receives selected record data only.. based on keys you can enabled/disabled - actions.
 Query-         |_ might not be availble - in feature means ? - query -- here both are same - so how to handle dynamic.



method implementation:

method get_instance_features.
  reuslt-%delete = 
endmethod.


get_instance_features- components:
 importing 
  |_keys  type structure for instance features key zr_dbt_trav\\r_dbt_Trav
  |_requested_features  type structure for instance features request zr_dbt_trav\\r_dbt_Trav
 changing
  |_ result type table for instance features result zr_dbt_trav\\r_dbt_Trav optional
  |_failed type response for failed early zr_dbt_Trav
  |_reported type response for reported early zr_dbt_Trav.
  





